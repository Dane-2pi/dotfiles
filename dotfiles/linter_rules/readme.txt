These should be placed in ./.github/linters/<rulesfiles> in order to be run by the default run_linters.sh 

These rules are set up to be mostly generally applicable, but may need tweaking. they should get tailored to the repo and commited if they are to be used regularly 